# Etapa 1 — Implementação Python (legado)

Código original da Etapa 1 (Flask + pytest), preservado para referência.

Para executar a Etapa 1:

```powershell
cd legacy/etapa1-python
python -m venv .venv
.\.venv\Scripts\pip install -r ../../requirements-etapa1.txt
# Ajuste PYTHONPATH conforme estrutura local
```

A **Etapa 2** utiliza TypeScript na raiz do projeto (`npm run dev`).
